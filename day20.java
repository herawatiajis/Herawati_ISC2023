package isc;
import java.util.Arrays;
public class day20 {

    public static void main(String[] args) {
        // Hari ke duapuluh dari 100 hari
        int[] angkaBulat = {5, 2, 9, 1, 5, 6};

        // mengurutkan item array secara ascending
        Arrays.sort(angkaBulat);

        // Menampilkan array yang telah di sorting
        System.out.println(Arrays.toString(angkaBulat));
    }
    
}
